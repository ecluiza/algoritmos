algoritmo "estrutura de repeticao para"

var
  contador, resp, numero: inteiro
inicio
  escreva("Informe um numero: ")
  leia(numero)

  para contador <- 1 ate 10 faca
   se(numero<>2)entao

   //resp <- numero*contador
   escreval(numero, " x ", contador, " = ",numero*contador)

   fimse
  fimpara
fimalgoritmo
